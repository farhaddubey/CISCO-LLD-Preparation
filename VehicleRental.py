from abc import ABC, abstractmethod
from enum import Enum
from typing import Optional

# Association : One object uses another object 
# Aggregation : One object has many objects, but they can live independently 
# Composition : One object strongly owns another object 
# Polymorphism : Same method name, different object behaviour 